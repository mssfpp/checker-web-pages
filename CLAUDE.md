# checker-web-pages

## Memoria

Usa **`.claude/memory/`** nella root di questo progetto come directory di memoria al posto di quella globale. Leggi e scrivi sempre lì — è versionata nel repo e condivisa tra macchine.
